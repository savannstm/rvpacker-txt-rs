{
	"name": "json-writer-electron",
	"version": "1.3.0",
	"description": "",
	"main": "./src/backend/main.js",
	"scripts": {
		"start": "electron .",
		"tw": "npx tailwindcss -i ./src/frontend/input.css -o ./src/frontend/output.css -w --minify",
		"build-dev": "electron-packager . --platform=win32 --arch=x64 --out=dist --overwrite",
		"build": "electron-packager . --platform=win32 --arch=x64 --out=dist --overwrite && electron-packager . --platform=win32 --arch=ia32 --out=dist --overwrite && electron-packager . --platform=linux --arch=x64 --out=dist --overwrite"
	},
	"author": "",
	"license": "WTFPL",
	"dependencies": {
		"electron-reloader": "^1.2.3",
		"fs-extra": "^11.2.0",
		"tailwindcss": "^3.4.1"
	},
	"devDependencies": {
		"@electron/packager": "^18.1.3",
		"electron": "^29.1.0"
	}
}
